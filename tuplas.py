# são semelhantes a listas, mas são IMUTÁVEIS

tupla = (1,2,3) # é declarado dentro de parênteses

# é possível colocar multiplos tipos de dados

#pegando dados

t[0] # pega o índice 0, da mesma forma q listas

tupla.index(2) # retorna o índice do valor 2
t.count(2) # retornar quantas vezes o valor 2 esta prsente na tupla

